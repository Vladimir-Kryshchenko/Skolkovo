name: baza-skolkovo

services:
  # Векторная БД
  qdrant:
    image: qdrant/qdrant:latest
    restart: unless-stopped
    ports:
      - "6333:6333"   # REST/Dashboard
      - "6334:6334"   # gRPC
    volumes:
      - qdrant_data:/qdrant/storage

  # Локальные эмбеддинги multilingual-e5 (HuggingFace Text Embeddings Inference)
  # CPU-образ. Для GPU использовать образ без суффикса -cpu и добавить deploy.resources.
  tei:
    image: ghcr.io/huggingface/text-embeddings-inference:cpu-1.7
    restart: unless-stopped
    command: ["--model-id", "intfloat/multilingual-e5-base"]
    environment:
      HF_ENDPOINT: https://huggingface.co
    ports:
      - "8081:80"
    volumes:
      - tei_data:/data

  # Реестр метаданных и БД админки
  postgres:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-skolkovo}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-skolkovo}
      POSTGRES_DB: ${POSTGRES_DB:-skolkovo}
    ports:
      - "5432:5432"
    volumes:
      - pg_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-skolkovo} -d ${POSTGRES_DB:-skolkovo}"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 20s

  # Сам сервис: планировщик + открытый MCP + админка
  skolkovo:
    build:
      context: ..
      dockerfile: deploy/Dockerfile
    restart: unless-stopped
    depends_on:
      postgres:
        condition: service_healthy   # ждём готовности БД перед миграциями/стартом
      qdrant:
        condition: service_started
      tei:
        condition: service_started
    environment:
      QDRANT_URL: http://qdrant:6333
      TEI_URL: http://tei:80
      POSTGRES_DSN: postgres://${POSTGRES_USER:-skolkovo}:${POSTGRES_PASSWORD:-skolkovo}@postgres:5432/${POSTGRES_DB:-skolkovo}?sslmode=disable
      STORE_BACKEND: postgres
      MCP_API_KEY: ${MCP_API_KEY:-change-me-please}
      ADMIN_USER: ${ADMIN_USER:-admin}
      ADMIN_PASSWORD: ${ADMIN_PASSWORD:-admin}
      NEWS_RSS_URL: ${NEWS_RSS_URL:-https://sk.ru/news/rss/}
      NOTIFY_WEBHOOK: ${NOTIFY_WEBHOOK:-}
      DOCS_DIR: /data/docs
      REGISTRY_PATH: /data/docs/Метаданные/реестр_документов.json
      REPORT_DIR: /data/reports
      CHROME_PATH: /usr/bin/chromium
    ports:
      - "8080:8080"   # MCP
      - "8090:8090"   # админка
      - "8091:8091"   # резидентство-админ
      - "8092:8092"   # портал клиента
      - "8093:8093"   # чат-виджет
      - "8094:8094"   # дашборд консультанта
      - "9090:9090"   # prometheus метрики
    volumes:
      - docs_data:/data/docs
      - reports_data:/data/reports
    healthcheck:
      test: ["CMD-SHELL", "curl -fsS http://localhost:8080/health || exit 1"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 40s

volumes:
  qdrant_data:
  tei_data:
  pg_data:
  docs_data:
  reports_data:

  # NGINX reverse-proxy (вынесен из-под compose, подключён к сети вручную)
  # Если хотите управлять через compose — раскомментируйте:
  # nginx:
  #   image: nginx:alpine
  #   restart: unless-stopped
  #   ports:
  #     - "80:80"
  #     - "443:443"
  #   volumes:
  #     - ./nginx.conf:/etc/nginx/conf.d/default.conf:ro
  #     - ./certs:/etc/nginx/certs:ro
  #   depends_on:
  #     - skolkovo
