---
name: MCP Server Configuration
description: MCP сервер для базы знаний Сколково - конфигурация и детали подключения
type: project
---

**MCP сервер настроен и работает**

- Endpoint: `https://213.136.75.7/mcp` (Streamable HTTP, удалённый сервер)
- API ключ: `517a4b18d8701532ce5e9d50671395b8602a9f9e68691f1d`
- Конфигурация Qwen Code: `.qwen/settings.json`
- Файл .env: содержит `MCP_API_KEY`

**Доступные инструменты MCP:**
- `search_documents` — семантический поиск по документам
- `get_document` — метаданные документа по id
- `list_active_documents` — список действующих документов

**Зависимости для полноценной работы:**
- Qdrant (порт 6333) — векторная БД
- TEI (порт 8081) — сервис эмбеддингов
- PostgreSQL (порт 5432) — реестр метаданных

**Why:** Для работы с документами Фонда «Сколково» через MCP нужен запущенный MCP сервер с правильным API ключом.

**How to apply:** При настройке проекта или при вопросах о MCP сервере — проверять что сервер запущен на `https://213.136.75.7/mcp`, и API ключ совпадает в `.env` и `.qwen/settings.json`. Использовать флаг `-k` для curl при работе с self-signed SSL сертификатом.
